name: snake_navigation_bar
description: Flutter bottom navigation bar widget with snake animation on change item.
version: 1.0.0+1

homepage: https://github.com/AllinMobile/SnakeBottomNavigationBar.git

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
